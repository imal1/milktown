# Milktown

一个所见即所得的本地 Markdown 编辑器，桌面应用，单文档模型。为作者本人日常写作而做，不为发布而做。

## Language

**文档**：
当前正在编辑的 Markdown 内容。同一时刻只存在一个，且只有一份——它活在编辑器里，应用的其他部分不持有副本。
_Avoid_: 内容、正文、笔记

**文件**：
磁盘上的一个 `.md` 实体，由绝对路径标识。文档来自文件、写回文件，但两者不是同一个东西：一个新建的文档还没有对应的文件。
_Avoid_: 文稿、document

**当前文件**：
当前文档对应的磁盘路径。新建但从未保存过的文档没有当前文件——这是「另存为」和「保存」行为分岔的唯一依据。
_Avoid_: 打开的文件、活动文件

**脏**：
文档自上次写回文件以来被修改过。由编辑器的文档变更事件置位，由一次成功的保存清零。不通过比对内容来判定。
_Avoid_: 未保存、已修改、dirty flag

**最近文件**：
曾被打开过的文件路径列表，按最近打开时间排序。它是本应用唯一的多文件入口——单文档模型下没有文件树，也没有标签页。
_Avoid_: 历史记录、最近文档

**编辑会话**：
一个窗口的生命周期内对一个文档的编辑过程。打开另一个文件即结束当前会话、开始新会话——撤销历史随之清空，这是刻意的，不是缺陷。
_Avoid_: 会话、session

## 版本

**版本**：
一个文件在某一时刻的完整内容留存。版本属于文件，不属于文档——新建但从未保存过的文档没有版本。版本存的是完整内容，不是相对上一版的增量。
_Avoid_: 快照、存档、提交、commit

**历史**：
一个文件的全部版本，按时间倒序排列。历史属于单个文件，文件之间的历史互不相干。
_Avoid_: 版本列表、时间线、log

**规范化**：
把 Markdown 文本解析成文档、再序列化回 Markdown 文本的过程。同一份内容经规范化后总是得到完全相同的文本，因此规范化后的文本是本应用唯一认可的书写形式。
_Avoid_: 格式化、美化、format

**双页视图**：
并排显示两份内容的全屏界面，进入后接管整个窗口，退出后回到写作。当前只用于对比同一文件的两个版本；对比任意两个不同文件属于后续阶段。
_Avoid_: 分屏、对比模式、diff 视图
